spring:
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

dubbo:
  registry:
    address: spring-cloud://localhost
  protocol:
    name: dubbo
    port: -1
  scan:
    base-packages: com.zz.nacos.server