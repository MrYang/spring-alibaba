server:
  port: 8603

spring:
  application:
    name: nacos-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

fegin:
  sentinel:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: '*'